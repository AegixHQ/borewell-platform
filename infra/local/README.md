# Local dev helpers

Seed scripts and local-only fixtures go here (e.g. a script to create a
demo contractor + a few resources so the apps aren't empty on first run).
Nothing here should be required for `make up` to work - it's for convenience
only.
