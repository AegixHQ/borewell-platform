## What does this PR do?


## AI-authored changes checklist
(Required for every PR, whether written by a human or an AI agent/assistant)

- [ ] Every endpoint/field this code calls is present in `packages/contracts/`
      — verified by opening the actual spec file, not assumed
- [ ] No new top-level folders or renamed existing folders (or: an ADR is
      linked below)
- [ ] Every new dependency is justified in this description, not just added
      because it seemed useful
- [ ] No commented-out code, no unused imports/variables, no speculative
      "might need this later" functions
- [ ] Tests were actually run locally and the real output is pasted below
- [ ] `python tools/contract-check/check_contract.py <service>` passes for
      any service touched
- [ ] `ruff check .` passes

## Test output

```
(paste actual output here)
```

## Linked ADR (if this touches repo structure)

